apiVersion: v1
kind: Namespace
metadata:
  name: disp-grp14
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: stock-service
  namespace: disp-grp14
spec:
  selector:
    matchLabels:
      app: stock-service
  template:
    metadata:
      labels:
        app: stock-service
    spec:
      containers:
      - name: stock-service
        image: dispgrp14/stock-service:v2.0
        imagePullPolicy: IfNotPresent
        resources:
          requests:
            memory: "50Mi"
            cpu: "50m"
          limits:
            memory: "100Mi"
            cpu: "100m"
        ports:
        - containerPort: 5000
        env:
          - name: DBConnection
            value: Server=mssql-deployment.default.svc.cluster.local;Database=grp14-StockService-db;User=SA;Password=F21swtdisp!!;
          - name: ASPNETCORE_URLS
            value: http://+:5000
          - name: HTTP1PORT
            value: "5000"
---   
apiVersion: v1
kind: Service
metadata:
  name: stock-service-svc
  namespace: disp-grp14
spec:
  selector:
    app: stock-service
  ports:
  - port: 5000 
    targetPort: 5000
